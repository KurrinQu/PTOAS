---
phase: 02-pto-lowering
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - include/PTO/Transforms/A5VMLowering.h
  - lib/PTO/Transforms/PTOToA5VM.cpp
  - lib/PTO/Transforms/CMakeLists.txt
autonomous: true
requirements:
  - PTO-01
  - PTO-02
  - PTO-03
  - PTO-04
must_haves:
  truths:
    - "The PTO-to-A5VM lowering surface exposes visible `lowerTLOAD`, `lowerTABS`, and `lowerTSTORE` entrypoints instead of one opaque rewrite body."
    - "Shared lowering contracts preserve the PTO-side decisions the user locked for load, unary, and store lowering."
    - "Unsupported TSTORE domains remain explicit and diagnosable instead of disappearing into implicit fallthrough logic."
  artifacts:
    - path: "include/PTO/Transforms/A5VMLowering.h"
      provides: "Shared Phase 2 lowering contracts and entrypoint declarations"
      contains: "struct A5VMLoadContract"
    - path: "lib/PTO/Transforms/PTOToA5VM.cpp"
      provides: "Contract extraction helpers, unary helper shape, and placeholder branch handling"
      min_lines: 180
    - path: "lib/PTO/Transforms/CMakeLists.txt"
      provides: "Build hook for PTOToA5VM.cpp inside PTOTransforms"
      contains: "PTOToA5VM.cpp"
  key_links:
    - from: "include/PTO/Transforms/A5VMLowering.h"
      to: "lib/PTO/Transforms/PTOToA5VM.cpp"
      via: "declared contracts implemented by extraction and lowering helpers"
      pattern: "A5VMLoadContract|A5VMUnaryContract|A5VMStoreContract"
    - from: "lib/PTO/Transforms/PTOToA5VM.cpp"
      to: "test/phase2/tabs_precheck.mlir"
      via: "exact lowering diagnostic strings and unary contract attrs"
      pattern: "TABS lowering requires|unary_family = \"abs\""
---

<objective>
Define the reusable lowering contracts and helper layer that Phase 2 will implement against.

Purpose: Preserve PTO template-dispatch inputs explicitly before pass wiring, so the lowering remains readable, extensible, and testable instead of collapsing into a one-off `Abs` rewrite.
Output: A public lowering-contract header, a new `PTOToA5VM.cpp` helper implementation file, and the build hook for it.
</objective>

<execution_context>
@/home/mouliangyu/.codex/get-shit-done/workflows/execute-plan.md
@/home/mouliangyu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pto-lowering/02-CONTEXT.md
@.planning/phases/02-pto-lowering/02-RESEARCH.md
@include/PTO/IR/PTO.h
@include/PTO/IR/PTOOps.td
@lib/PTO/IR/PTO.cpp
@include/PTO/Transforms/Passes.h
@lib/PTO/Transforms/PTOToEmitC.cpp
@lib/PTO/Transforms/CMakeLists.txt
@test/phase2/tload_contract_trace.mlir
@test/phase2/tstore_branch_shape.mlir
@test/phase2/tabs_precheck.mlir
@test/phase2/unary_template_shape.mlir

<interfaces>
From `include/PTO/IR/PTOOps.td`:
```tablegen
def TLoadOp  : PTO_TOp<"tload"> {
  let arguments = (ins
    PTODpsType:$src,
    PTODpsType:$dst,
    OptionalAttr<PTO_PadModeAttr>:$pad_mode,
    Optional<AnyType>:$pad_value,
    Optional<Index>:$left_padding_num,
    Optional<AnyType>:$right_padding_num,
    DefaultValuedOptionalAttr<BoolAttr, "false">:$init_out_buffer,
    Optional<AnyType>:$init_condition
  );
}

def TStoreOp : PTO_TOp<"tstore"> {
  let arguments = (ins PTODpsType:$src, PTODpsType:$dst);
}

def TAbsOp : PTO_TOp<"tabs"> {
  let arguments = (ins PTODpsType:$src, PTODpsType:$dst);
}
```

From `lib/PTO/IR/PTO.cpp`:
```c++
LogicalResult TLoadOp::verify();
LogicalResult TStoreOp::verify();
LogicalResult pto::TAbsOp::verify();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define public PTO-to-A5VM lowering contracts and entrypoints</name>
  <read_first>
    include/PTO/Transforms/A5VMLowering.h
    include/PTO/IR/PTO.h
    include/PTO/IR/PTOOps.td
    include/PTO/Transforms/Passes.h
    .planning/phases/02-pto-lowering/02-CONTEXT.md
  </read_first>
  <files>include/PTO/Transforms/A5VMLowering.h</files>
  <action>Create `include/PTO/Transforms/A5VMLowering.h` and declare the exact shared contract layer for Phase 2 in namespace `mlir::pto`. Define `enum class A5VMTileDomain { Vec, Acc, Mat };`, `struct A5VMPartitionTrace { SmallVector<int64_t> offsets; SmallVector<int64_t> sizes; bool hasDynamicOffsets; bool hasDynamicSizes; };`, `struct A5VMLoadContract`, `struct A5VMUnaryContract`, and `struct A5VMStoreContract`. `A5VMLoadContract` must contain these fields by name: `layout`, `srcShape`, `srcStrides`, `tileLayout`, `tileDomain`, `validRows`, `validCols`, `padMode`, `hasPadValue`, `leftPaddingPresent`, `rightPaddingPresent`, `initOutBuffer`, `hasInitCondition`, and `trace`. `A5VMUnaryContract` must contain `family`, `tileDomain`, `tileLayout`, `validRows`, `validCols`, and `elementType`. `A5VMStoreContract` must contain `srcDomain`, `dstLayout`, `dstShape`, `dstStrides`, `validRows`, `validCols`, and `trace`. Declare these exact lowering entrypoints: `LogicalResult lowerTLOAD(TLoadOp op, PatternRewriter &rewriter);`, `LogicalResult lowerTABS(TAbsOp op, PatternRewriter &rewriter);`, and `LogicalResult lowerTSTORE(TStoreOp op, PatternRewriter &rewriter);`. Also declare the shared unary helper signature `LogicalResult lowerUnaryTileOp(StringRef family, const A5VMUnaryContract &contract, Value src, Value dst, PatternRewriter &rewriter, Location loc);` so later unary ops can reuse the same path.</action>
  <acceptance_criteria>
    rg -n 'enum class A5VMTileDomain|struct A5VMPartitionTrace|struct A5VMLoadContract|struct A5VMUnaryContract|struct A5VMStoreContract' include/PTO/Transforms/A5VMLowering.h
    rg -n 'layout;|srcShape;|srcStrides;|tileLayout;|tileDomain;|validRows;|validCols;|padMode;|hasPadValue;|leftPaddingPresent;|rightPaddingPresent;|initOutBuffer;|hasInitCondition;|trace;' include/PTO/Transforms/A5VMLowering.h
    rg -n 'lowerTLOAD|lowerTABS|lowerTSTORE|lowerUnaryTileOp' include/PTO/Transforms/A5VMLowering.h
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n "struct A5VMLoadContract|struct A5VMUnaryContract|struct A5VMStoreContract" include/PTO/Transforms/A5VMLowering.h &amp;&amp; rg -n "lowerTLOAD|lowerTABS|lowerTSTORE|lowerUnaryTileOp" include/PTO/Transforms/A5VMLowering.h'</automated>
  </verify>
  <done>The source tree contains a public lowering-contract header that names the exact preserved metadata and the three visible PTO entrypoints required by the user’s locked decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Implement shared contract extraction helpers and placeholder branch behavior</name>
  <read_first>
    lib/PTO/Transforms/PTOToA5VM.cpp
    lib/PTO/Transforms/CMakeLists.txt
    include/PTO/Transforms/A5VMLowering.h
    lib/PTO/Transforms/PTOToEmitC.cpp
    lib/PTO/IR/PTO.cpp
    test/phase2/tload_contract_trace.mlir
    test/phase2/tstore_branch_shape.mlir
    test/phase2/tabs_precheck.mlir
    test/phase2/unary_template_shape.mlir
  </read_first>
  <files>lib/PTO/Transforms/PTOToA5VM.cpp, lib/PTO/Transforms/CMakeLists.txt</files>
  <action>Create `lib/PTO/Transforms/PTOToA5VM.cpp` and add it to `PTOTransforms` in `lib/PTO/Transforms/CMakeLists.txt`. In that file, implement shared helpers with these exact names: `extractPartitionTrace`, `extractTLoadContract`, `extractTAbsContract`, `extractTStoreContract`, `attachLoadContractAttrs`, `attachUnaryContractAttrs`, and `attachStoreContractAttrs`. Use `PartitionViewOp` offsets and sizes to populate `trace.offsets` and `trace.sizes`; populate `srcShape` and `srcStrides` from the logical tensor-view/memref source; derive `tileDomain` or `srcDomain` from `TileBufType` memory space or memref address space; and preserve the `TLoadOp` option presence flags in these exact booleans: `hasPadValue`, `leftPaddingPresent`, `rightPaddingPresent`, `initOutBuffer`, and `hasInitCondition`. Implement unary prechecks so failures emit these exact strings: `TABS lowering requires tile domain vec`, `TABS lowering requires row-major tile layout`, `TABS lowering requires matching valid rows and valid cols`, and `TABS lowering supports only f16 and f32 element types`. Implement explicit unsupported store-domain helpers that fail with the exact strings `TSTORE ACC lowering TODO for a5vm backend` and `TSTORE MAT lowering TODO for a5vm backend`; do not hide these branches behind a shared generic error. Implement `lowerUnaryTileOp` so it always attaches `unary_family`, `tile_domain`, `tile_layout`, `valid_rows`, and `valid_cols` as attributes on the future `a5vm.abs` op.</action>
  <acceptance_criteria>
    rg -n 'extractPartitionTrace|extractTLoadContract|extractTAbsContract|extractTStoreContract|attachLoadContractAttrs|attachUnaryContractAttrs|attachStoreContractAttrs|lowerUnaryTileOp' lib/PTO/Transforms/PTOToA5VM.cpp
    rg -n 'hasPadValue|leftPaddingPresent|rightPaddingPresent|initOutBuffer|hasInitCondition|trace\\.offsets|trace\\.sizes' lib/PTO/Transforms/PTOToA5VM.cpp
    rg -n 'TABS lowering requires tile domain vec|TABS lowering requires row-major tile layout|TABS lowering requires matching valid rows and valid cols|TABS lowering supports only f16 and f32 element types' lib/PTO/Transforms/PTOToA5VM.cpp
    rg -n 'TSTORE ACC lowering TODO for a5vm backend|TSTORE MAT lowering TODO for a5vm backend|PTOToA5VM\\.cpp' lib/PTO/Transforms/PTOToA5VM.cpp lib/PTO/Transforms/CMakeLists.txt
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n "extractTLoadContract|extractTAbsContract|extractTStoreContract|lowerUnaryTileOp" lib/PTO/Transforms/PTOToA5VM.cpp &amp;&amp; rg -n "TSTORE ACC lowering TODO for a5vm backend|TSTORE MAT lowering TODO for a5vm backend" lib/PTO/Transforms/PTOToA5VM.cpp &amp;&amp; rg -n "PTOToA5VM\\.cpp" lib/PTO/Transforms/CMakeLists.txt'</automated>
  </verify>
  <done>The repo contains a build-wired helper implementation that extracts and names the preserved Phase 2 metadata explicitly, emits the exact required TABS diagnostics, and keeps unsupported ACC/MAT store branches visible with dedicated TODO errors.</done>
</task>

</tasks>

<verification>
`bash -lc 'rg -n "struct A5VMLoadContract|struct A5VMUnaryContract|struct A5VMStoreContract" include/PTO/Transforms/A5VMLowering.h && rg -n "extractTLoadContract|extractTAbsContract|extractTStoreContract|lowerUnaryTileOp" lib/PTO/Transforms/PTOToA5VM.cpp && rg -n "PTOToA5VM\\.cpp" lib/PTO/Transforms/CMakeLists.txt'`
</verification>

<success_criteria>
The Phase 2 lowering architecture is written down in code before pass wiring: the contracts name the preserved PTO decisions, the helper file implements extraction and exact diagnostics, and the unsupported `ACC`/`MAT` store branches remain explicit.
</success_criteria>

<output>
After completion, create `.planning/phases/02-pto-lowering/02-pto-lowering-02-SUMMARY.md`
</output>
