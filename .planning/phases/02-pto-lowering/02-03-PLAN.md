---
phase: 02-pto-lowering
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - include/PTO/Transforms/Passes.h
  - include/PTO/Transforms/Passes.td
  - lib/PTO/Transforms/PTOToA5VM.cpp
  - tools/ptoas/ptoas.cpp
autonomous: true
requirements:
  - PTO-01
  - PTO-02
  - PTO-03
  - PTO-04
must_haves:
  truths:
    - "Running `ptoas --pto-backend=a5vm` lowers PTO `tload`, `tabs`, and `tstore` into `a5vm` instead of passing them to the legacy EmitC backend."
    - "The lowered `a5vm` ops carry explicit metadata for load, unary, and store decisions needed for backend code selection."
    - "Future PTO unary ops or load/store variants can add cases through the same lowering pass without replacing the backend architecture."
  artifacts:
    - path: "include/PTO/Transforms/Passes.td"
      provides: "registered PTO-to-A5VM pass contract"
      contains: "pto-to-a5vm"
    - path: "include/PTO/Transforms/Passes.h"
      provides: "factory declaration for createLowerPTOToA5VMPass"
      contains: "createLowerPTOToA5VMPass"
    - path: "lib/PTO/Transforms/PTOToA5VM.cpp"
      provides: "lowering pass, conversion patterns, and attribute attachment"
      min_lines: 260
    - path: "tools/ptoas/ptoas.cpp"
      provides: "backend branch that runs the PTO-to-A5VM pass before textual emission"
      contains: "createLowerPTOToA5VMPass"
  key_links:
    - from: "tools/ptoas/ptoas.cpp"
      to: "lib/PTO/Transforms/PTOToA5VM.cpp"
      via: "backend=a5vm branch adds createLowerPTOToA5VMPass before textual emission"
      pattern: "createLowerPTOToA5VMPass"
    - from: "lib/PTO/Transforms/PTOToA5VM.cpp"
      to: "test/phase2/tload_contract_trace.mlir"
      via: "attached load attrs checked by the Phase 2 fixtures"
      pattern: "src_shape|src_strides|trace_offsets|trace_sizes"
---

<objective>
Implement the actual PTO-to-A5VM lowering pass and wire it into the A5VM backend path.

Purpose: Turn the shared Phase 2 contracts into real `a5vm` ops at the current backend boundary, while keeping the visible entrypoints and shared helper structure the user asked for.
Output: A registered `pto-to-a5vm` pass, conversion patterns using `lowerTLOAD`/`lowerTABS`/`lowerTSTORE`, and `ptoas` wiring that runs the pass when `--pto-backend=a5vm`.
</objective>

<execution_context>
@/home/mouliangyu/.codex/get-shit-done/workflows/execute-plan.md
@/home/mouliangyu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pto-lowering/02-CONTEXT.md
@.planning/phases/02-pto-lowering/02-RESEARCH.md
@.planning/phases/01-a5vm-foundation/01-02-PLAN.md
@.planning/phases/01-a5vm-foundation/01-03-PLAN.md
@include/PTO/Transforms/A5VMLowering.h
@include/PTO/Transforms/Passes.h
@include/PTO/Transforms/Passes.td
@lib/PTO/Transforms/PTOToA5VM.cpp
@tools/ptoas/ptoas.cpp
@test/phase2/tload_contract_trace.mlir
@test/phase2/tstore_branch_shape.mlir
@test/phase2/tabs_precheck.mlir
@test/phase2/unary_template_shape.mlir

<interfaces>
From `include/PTO/Transforms/A5VMLowering.h`:
```c++
LogicalResult lowerTLOAD(TLoadOp op, PatternRewriter &rewriter);
LogicalResult lowerTABS(TAbsOp op, PatternRewriter &rewriter);
LogicalResult lowerTSTORE(TStoreOp op, PatternRewriter &rewriter);
LogicalResult lowerUnaryTileOp(StringRef family, const A5VMUnaryContract &contract,
                               Value src, Value dst, PatternRewriter &rewriter, Location loc);
```

From `include/PTO/Transforms/Passes.h`:
```c++
std::unique_ptr<Pass> createEmitPTOManualPass(PTOArch arch);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register and implement the PTO-to-A5VM lowering pass</name>
  <read_first>
    include/PTO/Transforms/Passes.h
    include/PTO/Transforms/Passes.td
    lib/PTO/Transforms/PTOToA5VM.cpp
    include/PTO/Transforms/A5VMLowering.h
    .planning/phases/02-pto-lowering/02-CONTEXT.md
    test/phase2/tload_contract_trace.mlir
    test/phase2/tstore_branch_shape.mlir
    test/phase2/tabs_precheck.mlir
    test/phase2/unary_template_shape.mlir
  </read_first>
  <files>include/PTO/Transforms/Passes.h, include/PTO/Transforms/Passes.td, lib/PTO/Transforms/PTOToA5VM.cpp</files>
  <action>In `include/PTO/Transforms/Passes.td`, add a new pass definition named exactly `PTOToA5VM` with argument string `pto-to-a5vm`, operation type `ModuleOp`, constructor `mlir::pto::createLowerPTOToA5VMPass()`, and dependent dialects `pto::PTODialect`, `pto::a5vm::A5VMDialect`, `func::FuncDialect`, `arith::ArithDialect`, and `memref::MemRefDialect`. Declare `std::unique_ptr<Pass> createLowerPTOToA5VMPass();` in `include/PTO/Transforms/Passes.h`. In `lib/PTO/Transforms/PTOToA5VM.cpp`, add the pass implementation and the conversion patterns `PTOTLoadToA5VMLoad`, `PTOTAbsToA5VMAbs`, and `PTOTStoreToA5VMStore`, each calling the visible helper entrypoints `lowerTLOAD`, `lowerTABS`, and `lowerTSTORE`. Implement the lowerings with these exact A5VM attribute names and values. `lowerTLOAD` must create `a5vm.load` and attach `layout`, `src_shape`, `src_strides`, `tile_layout`, `tile_domain`, `valid_rows`, `valid_cols`, `pad_mode`, `has_pad_value`, `left_padding_present`, `right_padding_present`, `init_out_buffer`, `has_init_condition`, `trace_offsets`, and `trace_sizes`. `lowerTABS` must call `lowerUnaryTileOp("abs", ...)` so the emitted `a5vm.abs` carries `unary_family = "abs"`, `tile_domain = "vec"`, `tile_layout = "row_major"`, `valid_rows`, and `valid_cols`. `lowerTSTORE` must create `a5vm.store` for the vec branch with attributes `src_domain`, `dst_layout`, `dst_shape`, `dst_strides`, `valid_rows`, `valid_cols`, `trace_offsets`, and `trace_sizes`; for `ACC` and `MAT`, keep the explicit TODO failure branches from Plan 02. Do not emit any `emitc::CallOpaqueOp` for `TLOAD`, `TABS`, or `TSTORE` in this pass.</action>
  <acceptance_criteria>
    rg -n 'pto-to-a5vm|createLowerPTOToA5VMPass|pto::a5vm::A5VMDialect' include/PTO/Transforms/Passes.h include/PTO/Transforms/Passes.td
    rg -n 'PTOTLoadToA5VMLoad|PTOTAbsToA5VMAbs|PTOTStoreToA5VMStore|lowerTLOAD\\(|lowerTABS\\(|lowerTSTORE\\(' lib/PTO/Transforms/PTOToA5VM.cpp
    rg -n 'src_shape|src_strides|pad_mode|has_pad_value|left_padding_present|right_padding_present|init_out_buffer|has_init_condition|trace_offsets|trace_sizes' lib/PTO/Transforms/PTOToA5VM.cpp
    rg -n 'unary_family = "abs"|src_domain|dst_layout|emitc::CallOpaqueOp' lib/PTO/Transforms/PTOToA5VM.cpp
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'ninja -C build PTOTransforms ptoas &amp;&amp; bash test/phase2/run_phase2_checks.sh'</automated>
  </verify>
  <done>The project has a registered `pto-to-a5vm` pass that lowers `TLOAD`, `TABS`, and the vec-path `TSTORE` into `a5vm` with the exact metadata required by the Phase 2 fixtures, while leaving explicit TODO failures for unsupported `ACC` and `MAT` store branches.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the A5VM backend branch to run PTO-to-A5VM lowering</name>
  <read_first>
    tools/ptoas/ptoas.cpp
    include/PTO/Transforms/Passes.h
    include/PTO/Transforms/Passes.td
    .planning/phases/01-a5vm-foundation/01-03-PLAN.md
    test/phase2/run_phase2_checks.sh
  </read_first>
  <files>tools/ptoas/ptoas.cpp</files>
  <action>Update `tools/ptoas/ptoas.cpp` so the `--pto-backend=a5vm` path runs the same pre-backend passes as the current flow, then adds `pto::createLowerPTOToA5VMPass()` before textual A5VM emission. Keep `--pto-backend=emitc` on the existing `createEmitPTOManualPass(...)`, `emitc::createFormExpressionsPass()`, and `emitc::translateToCpp(...)` path. In the A5VM branch, do not add `createEmitPTOManualPass(...)` or `emitc::createFormExpressionsPass()`. Instead, run `pto::createLowerPTOToA5VMPass()`, keep the post-pass `createCSEPass()` cleanup, and then call the existing A5VM textual emitter branch from Phase 1. Preserve `--a5vm-print-ir` behavior so the Phase 2 fixtures can inspect lowered A5VM IR, and keep the backend flag default unchanged. The final effect must be that `pto.tload`, `pto.tabs`, and `pto.tstore` reach the emitter as `a5vm` ops with the exact metadata attrs from Task 1, not as legacy `emitc.call_opaque` intrinsics.</action>
  <acceptance_criteria>
    rg -n 'createLowerPTOToA5VMPass|pto-backend|a5vm-print-ir' tools/ptoas/ptoas.cpp
    rg -n 'createEmitPTOManualPass|emitc::createFormExpressionsPass|emitc::translateToCpp' tools/ptoas/ptoas.cpp
    bash -lc './build/tools/ptoas/ptoas --pto-backend=a5vm --a5vm-print-ir test/phase2/tload_contract_trace.mlir -o /dev/null 2>&1 | FileCheck test/phase2/tload_contract_trace.mlir'
    bash -lc './build/tools/ptoas/ptoas --pto-backend=a5vm --a5vm-print-ir test/phase2/unary_template_shape.mlir -o /dev/null 2>&1 | FileCheck test/phase2/unary_template_shape.mlir'
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'ninja -C build PTOTransforms ptoas &amp;&amp; bash test/phase2/run_phase2_checks.sh &amp;&amp; ./build/tools/ptoas/ptoas --pto-backend=a5vm --a5vm-print-ir test/phase2/unary_template_shape.mlir -o /dev/null 2&gt;&amp;1 | FileCheck test/phase2/unary_template_shape.mlir'</automated>
  </verify>
  <done>The A5VM backend branch in `ptoas` runs PTO-to-A5VM lowering before textual emission, preserves the legacy EmitC path as the default backend, and exposes lowered `a5vm` metadata to the Phase 2 verification fixtures.</done>
</task>

</tasks>

<verification>
`bash -lc 'ninja -C build PTOTransforms ptoas && bash test/phase2/run_phase2_checks.sh'`
</verification>

<success_criteria>
`ptoas --pto-backend=a5vm` lowers the `Abs`-path PTO ops into `a5vm` with preserved semantics and explicit metadata, `ACC` and `MAT` store branches remain visible TODO failures, and the same architecture now has a clear extension point for future unary or load/store variants.
</success_criteria>

<output>
After completion, create `.planning/phases/02-pto-lowering/02-pto-lowering-03-SUMMARY.md`
</output>
