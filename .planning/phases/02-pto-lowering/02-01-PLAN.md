---
phase: 02-pto-lowering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/phase2/tload_contract_trace.mlir
  - test/phase2/tstore_branch_shape.mlir
  - test/phase2/tabs_precheck.mlir
  - test/phase2/unary_template_shape.mlir
  - test/phase2/run_phase2_checks.sh
autonomous: true
requirements:
  - PTO-01
  - PTO-02
  - PTO-03
  - PTO-04
must_haves:
  truths:
    - "Developers can run committed source fixtures that define the exact A5VM metadata and diagnostics expected from PTO lowering."
    - "Each Phase 2 requirement has a concrete automated command instead of a Wave 0 placeholder."
    - "The Phase 2 verification loop checks semantic preservation and explicit unsupported-branch behavior before lowering code is written."
  artifacts:
    - path: "test/phase2/tload_contract_trace.mlir"
      provides: "TLOAD preservation fixture for layout, shape, valid-region, padding/init flags, and partition trace"
      min_lines: 25
    - path: "test/phase2/tstore_branch_shape.mlir"
      provides: "TSTORE vec-path lowering fixture plus ACC/MAT placeholder diagnostics"
      min_lines: 25
    - path: "test/phase2/tabs_precheck.mlir"
      provides: "TABS lowering precheck fixture for domain, layout, dtype, and valid-shape rules"
      min_lines: 20
    - path: "test/phase2/unary_template_shape.mlir"
      provides: "Reusable unary-lowering fixture for the first TABS-backed unary template"
      min_lines: 20
    - path: "test/phase2/run_phase2_checks.sh"
      provides: "Documented FileCheck invocation path for all Phase 2 fixtures"
      min_lines: 20
  key_links:
    - from: "test/phase2/tload_contract_trace.mlir"
      to: "./build/tools/ptoas/ptoas"
      via: "a5vm debug-IR output checked with FileCheck"
      pattern: "a5vm\\.load|trace_offsets|trace_sizes"
    - from: "test/phase2/tstore_branch_shape.mlir"
      to: "./build/tools/ptoas/ptoas"
      via: "vec-path lowering plus explicit ACC/MAT TODO diagnostics"
      pattern: "src_domain = \"vec\"|ACC lowering TODO|MAT lowering TODO"
---

<objective>
Create the committed validation fixtures and runner for Phase 2 before semantic-lowering work starts.

Purpose: Replace the current Wave 0 validation gaps with concrete source tests that lock the exact A5VM metadata, branch visibility, and diagnostic strings required for PTO lowering.
Output: Four `test/phase2/*.mlir` fixtures plus one executable `run_phase2_checks.sh` helper.
</objective>

<execution_context>
@/home/mouliangyu/.codex/get-shit-done/workflows/execute-plan.md
@/home/mouliangyu/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-pto-lowering/02-CONTEXT.md
@.planning/phases/02-pto-lowering/02-RESEARCH.md
@.planning/phases/02-pto-lowering/02-VALIDATION.md
@.planning/codebase/TESTING.md
@.planning/phases/01-a5vm-foundation/01-02-PLAN.md
@.planning/phases/01-a5vm-foundation/01-03-PLAN.md
@test/samples/Abs/abs.py
@include/PTO/IR/PTOOps.td
@tools/ptoas/ptoas.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Phase 2 MLIR/FileCheck fixtures</name>
  <read_first>
    test/phase2/tload_contract_trace.mlir
    test/phase2/tstore_branch_shape.mlir
    test/phase2/tabs_precheck.mlir
    test/phase2/unary_template_shape.mlir
    .planning/phases/02-pto-lowering/02-CONTEXT.md
    .planning/phases/02-pto-lowering/02-VALIDATION.md
    test/samples/Abs/abs.py
    include/PTO/IR/PTOOps.td
    tools/ptoas/ptoas.cpp
  </read_first>
  <files>test/phase2/tload_contract_trace.mlir, test/phase2/tstore_branch_shape.mlir, test/phase2/tabs_precheck.mlir, test/phase2/unary_template_shape.mlir</files>
  <action>Create four new source tests under `test/phase2/` with `// RUN:` lines that use `./build/tools/ptoas/ptoas --pto-backend=a5vm` and `FileCheck`. In `tload_contract_trace.mlir`, model the `Abs`-style `pto.tload` path and check that the lowered `a5vm.load` carries these exact metadata keys and values in the IR dump: `layout = "nd"`, `src_shape = [32, 32]`, `src_strides = [32, 1]`, `tile_layout = "row_major"`, `tile_domain = "vec"`, `valid_rows = 32`, `valid_cols = 32`, `pad_mode = "none"`, `has_pad_value = false`, `left_padding_present = false`, `right_padding_present = false`, `init_out_buffer = false`, `has_init_condition = false`, `trace_offsets = [0, 0]`, and `trace_sizes = [32, 32]`. In `tstore_branch_shape.mlir`, check one successful vec-path lowering to `a5vm.store` with `src_domain = "vec"`, `dst_layout = "nd"`, `dst_shape = [32, 32]`, `dst_strides = [32, 1]`, `valid_rows = 32`, `valid_cols = 32`, `trace_offsets = [0, 0]`, and `trace_sizes = [32, 32]`, plus two negative cases whose stderr contains the exact strings `TSTORE ACC lowering TODO for a5vm backend` and `TSTORE MAT lowering TODO for a5vm backend`. In `tabs_precheck.mlir`, include one successful `pto.tabs` case and three negative cases whose diagnostics contain the exact strings `TABS lowering requires tile domain vec`, `TABS lowering requires row-major tile layout`, `TABS lowering requires matching valid rows and valid cols`, and `TABS lowering supports only f16 and f32 element types`. In `unary_template_shape.mlir`, check that the lowered `a5vm.abs` carries `unary_family = "abs"`, `tile_domain = "vec"`, `tile_layout = "row_major"`, `valid_rows = 32`, and `valid_cols = 32`, and also check `CHECK-NOT: emitc.call_opaque "TABS"` so the new lowering path is visibly distinct from the legacy EmitC lowering.</action>
  <acceptance_criteria>
    rg -n 'layout = "nd"|src_shape = \\[32, 32\\]|src_strides = \\[32, 1\\]|trace_offsets = \\[0, 0\\]|trace_sizes = \\[32, 32\\]' test/phase2/tload_contract_trace.mlir
    rg -n 'src_domain = "vec"|dst_layout = "nd"|TSTORE ACC lowering TODO for a5vm backend|TSTORE MAT lowering TODO for a5vm backend' test/phase2/tstore_branch_shape.mlir
    rg -n 'TABS lowering requires tile domain vec|TABS lowering requires row-major tile layout|TABS lowering requires matching valid rows and valid cols|TABS lowering supports only f16 and f32 element types' test/phase2/tabs_precheck.mlir
    rg -n 'unary_family = "abs"|tile_domain = "vec"|tile_layout = "row_major"|CHECK-NOT: emitc.call_opaque "TABS"' test/phase2/unary_template_shape.mlir
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'test -f test/phase2/tload_contract_trace.mlir &amp;&amp; test -f test/phase2/tstore_branch_shape.mlir &amp;&amp; test -f test/phase2/tabs_precheck.mlir &amp;&amp; test -f test/phase2/unary_template_shape.mlir &amp;&amp; rg -n "RUN: .*--pto-backend=a5vm" test/phase2/*.mlir'</automated>
  </verify>
  <done>The repository contains four Phase 2 fixtures whose contents lock the exact lowering metadata, precheck diagnostics, and explicit unsupported-branch behavior required by PTO-01 through PTO-04.</done>
</task>

<task type="auto">
  <name>Task 2: Add a documented runner for all Phase 2 checks</name>
  <read_first>
    test/phase2/run_phase2_checks.sh
    .planning/phases/02-pto-lowering/02-VALIDATION.md
    test/phase1/run_phase1_checks.sh
  </read_first>
  <files>test/phase2/run_phase2_checks.sh</files>
  <action>Create `test/phase2/run_phase2_checks.sh` as an executable Bash script with `#!/usr/bin/env bash` and `set -euo pipefail`. Resolve the compiler binary as `./build/tools/ptoas/ptoas` and fail with `error: missing ./build/tools/ptoas/ptoas` if it does not exist. Run these five checks in order, printing `phase2 check: <fixture>` before each compiler invocation: `./build/tools/ptoas/ptoas --pto-backend=a5vm --a5vm-print-ir test/phase2/tload_contract_trace.mlir -o /dev/null 2>&1 | FileCheck test/phase2/tload_contract_trace.mlir`, `./build/tools/ptoas/ptoas --pto-backend=a5vm --a5vm-print-ir test/phase2/tstore_branch_shape.mlir -o /dev/null 2>&1 | FileCheck test/phase2/tstore_branch_shape.mlir`, `./build/tools/ptoas/ptoas --pto-backend=a5vm test/phase2/tabs_precheck.mlir -o /dev/null 2>&1 | FileCheck test/phase2/tabs_precheck.mlir`, `./build/tools/ptoas/ptoas --pto-backend=a5vm --a5vm-print-ir test/phase2/unary_template_shape.mlir -o /dev/null 2>&1 | FileCheck test/phase2/unary_template_shape.mlir`, and `ctest --test-dir build --output-on-failure`. Do not use watch mode or lit discovery; the script must be a direct, committed invocation path for this phase.</action>
  <acceptance_criteria>
    rg -n 'set -euo pipefail|missing \\./build/tools/ptoas/ptoas|phase2 check:' test/phase2/run_phase2_checks.sh
    rg -n 'tload_contract_trace\\.mlir|tstore_branch_shape\\.mlir|tabs_precheck\\.mlir|unary_template_shape\\.mlir' test/phase2/run_phase2_checks.sh
    rg -n 'ctest --test-dir build --output-on-failure|--pto-backend=a5vm|--a5vm-print-ir' test/phase2/run_phase2_checks.sh
    test -x test/phase2/run_phase2_checks.sh
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'test -x test/phase2/run_phase2_checks.sh &amp;&amp; rg -n "phase2 check:|FileCheck test/phase2/" test/phase2/run_phase2_checks.sh &amp;&amp; rg -n "ctest --test-dir build --output-on-failure|--pto-backend=a5vm|--a5vm-print-ir" test/phase2/run_phase2_checks.sh'</automated>
  </verify>
  <done>Phase 2 has a committed, executable verification entrypoint that runs every planned source fixture and the build-tree smoke suite without relying on external lit configuration.</done>
</task>

</tasks>

<verification>
`bash -lc 'test -x test/phase2/run_phase2_checks.sh && rg -n "FileCheck test/phase2/" test/phase2/run_phase2_checks.sh && rg -n "ctest --test-dir build --output-on-failure|--pto-backend=a5vm" test/phase2/run_phase2_checks.sh && rg -n "RUN: .*--pto-backend=a5vm" test/phase2/*.mlir'`
</verification>

<success_criteria>
Every Phase 2 requirement in the roadmap has a corresponding committed source fixture, and the repository contains one executable runner that spells out the exact verification commands for the wave.
</success_criteria>

<output>
After completion, create `.planning/phases/02-pto-lowering/02-pto-lowering-01-SUMMARY.md`
</output>
